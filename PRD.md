# PRD — Product Requirements Document
## Kanban Board

---

## 1. 개요

| 항목 | 내용 |
|------|------|
| 제품명 | Kanban Board |
| 버전 | 1.0.0 |
| 작성일 | 2026-05-13 |
| 대상 | KOSA 바이브 코딩 day03 실습 |

### 배경
할 일(To-Do), 진행 중(In Progress), 완료(Done) 세 가지 상태를 시각적으로 관리할 수 있는 경량 칸반보드가 필요하다. **복수 사용자가 각자의 보드를 독립적으로 사용**할 수 있어야 하며, 향후 Supabase 인증 연동을 통해 계정 기반 다중 사용자 환경으로 전환할 수 있는 구조로 설계한다.

### 목표
- 카드 단위로 작업을 등록·관리한다.
- 드래그 앤 드롭으로 카드를 컬럼 간 이동한다.
- 사용자별로 데이터가 격리되어 저장·복원된다.
- 인증 레이어 추가 시 코드 변경 범위를 데이터 레이어로 최소화한다.

---

## 2. 사용자

| 페르소나 | 설명 | v1 지원 |
|----------|------|---------|
| 게스트 사용자 | 로그인 없이 브라우저 로컬 스토리지로 개인 보드를 사용하는 사람 | ✅ |
| 인증 사용자 | Supabase 계정으로 로그인해 어느 기기에서나 보드에 접근하는 사람 | 🔜 v2 |

> **v1 범위**: 게스트 사용자만 지원. 데이터 모델과 스토리지 인터페이스는 인증 사용자를 대비한 구조로 설계한다.

---

## 3. 기능 요구사항

### F-01. 칸반 보드 표시
- 보드는 **To Do**, **In Progress**, **Done** 3개 컬럼으로 구성된다.
- 각 컬럼 헤더에 현재 카드 수를 badge로 표시한다.

### F-02. 카드 추가
- 각 컬럼 하단의 "+ Add card" 버튼을 누르면 인라인 입력 폼이 열린다.
- 텍스트 입력 후 확인 버튼(또는 Ctrl+Enter)으로 카드를 저장한다.
- 취소 버튼(또는 Esc)으로 입력을 취소한다.
- 빈 텍스트로는 카드를 저장할 수 없다.

### F-03. 카드 이동 (드래그 앤 드롭)
- 카드를 드래그해서 다른 컬럼의 카드 목록 영역에 드롭하면 해당 컬럼으로 이동된다.
- 드래그 중인 카드는 반투명(opacity 0.4)으로 표시된다.
- 드롭 가능한 컬럼은 배경색 변화로 강조된다.

### F-04. 카드 삭제
- 각 카드의 ✕ 버튼으로 해당 카드를 즉시 삭제한다.
- 삭제 확인 다이얼로그는 표시하지 않는다.

### F-05. 데이터 영속성 (사용자 격리)
- 카드 데이터는 **사용자 ID 단위로 격리**되어 저장된다.
- v1: `localStorage` 키를 `kanban-cards-{userId}` 형식으로 사용자별 분리.
- v2(예정): Supabase DB의 `user_id` 컬럼 + Row Level Security로 격리.
- 페이지 새로고침 후에도 마지막 상태가 복원된다.

### F-06. 초기 샘플 데이터
- 해당 사용자의 카드 데이터가 없는 첫 실행 시 샘플 카드 3개를 자동으로 표시한다.

### F-07. 라이트/다크 테마
- 상단 navbar의 토글 버튼으로 라이트·다크 테마를 전환한다.
- 선택한 테마는 `localStorage`에 저장되어 다음 방문 시 복원된다.
- OS 다크모드 설정을 초기값으로 반영한다.

### F-08. 사용자 식별 표시
- Navbar에 현재 사용자 식별자를 표시한다.
- v1: "Guest" 레이블 + 사용자 아이콘 표시.
- v2(예정): 로그인한 사용자의 이메일 또는 닉네임 + 로그아웃 메뉴 표시.

---

## 4. 비기능 요구사항

| 항목 | 요구사항 |
|------|---------|
| 실행 환경 | 빌드 도구 없이 `index.html`을 브라우저에서 직접 열기 (file:// 프로토콜) |
| 외부 의존성 | CDN 스크립트 태그만 허용 |
| 반응형 | 창 폭 축소 시 컬럼이 세로 배치로 전환 |
| 접근성 | 삭제 버튼에 `aria-label` 제공 |
| 브라우저 지원 | 최신 Chrome, Firefox, Safari, Edge |
| 확장성 | 스토리지 레이어 교체(localStorage → Supabase)가 `app.js` 내 Storage 객체만 수정으로 완료될 것 |

---

## 5. 향후 로드맵 (Out of Scope for v1)

| 기능 | 설명 |
|------|------|
| Supabase Auth 연동 | 이메일/소셜 로그인, JWT 세션 관리 |
| 서버 동기화 | Supabase Realtime으로 멀티 디바이스 동기화 |
| 카드 편집 | 생성 후 텍스트 수정 |
| 카드 내 순서 변경 | 같은 컬럼 내 드래그로 순서 조정 |
| 메타데이터 | 마감일, 담당자, 우선순위 |
| 보드 공유 | 팀 단위 협업 |
